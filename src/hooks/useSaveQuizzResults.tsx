// import { createQuizzResult } from "@/lib/quizz_result";
// import { findUserByEmail } from "@/lib/user";
// import { useEffect, useState } from "react";
// import { useSession } from "next-auth/react";
// import { useQuizzStateStore } from "@/store/useQuizzStateStore";

// export default function useSaveQuizzResults(){

//   const { data: session, status } = useSession();

//   // 1. Find user by email
//   // const user = await findUserByEmail(session?.user?.email);

//   // 2. Grab data from store

//   // 3. Create record in DB with the data

//   // 4. Returns the saved data back to the view for usage

//   return {loading, error, data}
// }
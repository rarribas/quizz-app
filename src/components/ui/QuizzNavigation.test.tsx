describe('Question Navigation', () => {
  it('disables prev button if first question', () => {
    // To be implemented
  });

  it('disables last button if last question', () => {
    // To be implemented
  });
});
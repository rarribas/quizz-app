{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["cypress", "@testing-library/cypress", "node"]
  },
  "include": [
    "e2e/**/*.ts",
    "support/**/*.ts",
    "*.ts"
  ],
  "exclude": ["../node_modules"]  // **do NOT exclude ../cypress**, otherwise TS says no inputs found
}
